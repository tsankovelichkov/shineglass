import React from 'react'
import Navigation from './Navigation';

const Categories = ({
    match
}) => {
    console.log(match)
    return (
        <>
          <Navigation/>
        </>
    )
}

export default Categories
